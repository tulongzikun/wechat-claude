# 复制为 .env 并填入你自己的值（.env 不纳入版本控制）。

# 你的微信 user_id：首次给 bot 发一条消息后，在 bot.log 的入站消息里看 from_user_id。
# 列在其中的 user_id 用全工具（Bash/Write/Edit），其他人只读。多个用逗号分隔。
WECHAT_ADMIN_USERS=

# 可选：企业微信群机器人 webhook（群聊「···」→群机器人→添加后复制完整地址）。
# 配置后 jobs/ 的定时推送会同时发企微群（无条件主动推）+ 个人微信（ilink）；
# 留空则只走 ilink（需 24h 内给 bot 发过消息，否则推送不出去）。
# 想推多个群：逗号分隔多个地址（下面所有 WEBHOOK 变量同此规则）。
# WECOM_WEBHOOK=https://qyapi.weixin.qq.com/cgi-bin/webhook/send?key=xxxxxxxx
# WECOM_WEBHOOK=https://...key=aaa,https://...key=bbb

# 可选：按任务覆盖 webhook——每个定时任务推到不同的群（同样支持逗号分隔多个）。
# 解析顺序：任务专用变量 → 上面的 WECOM_WEBHOOK 兜底；专用变量留空即回落。
# 所有 WECOM_WEBHOOK* 变量的值都支持行内 ` #` 注释（# 及之后忽略）。
# WECOM_WEBHOOK_DAILY=<仓库每日摘要群>     # 每天 17:00 项目更新
# WECOM_WEBHOOK_PAPERS=<每周论文速递群>    # 每周一 10:00 arXiv Top10
# WECOM_WEBHOOK_GAMES=<游戏资讯群>         # 每周二 10:00 米哈游游戏资讯
# WECOM_WEBHOOK_MACRO=<宏观周报群>         # 每周四 10:00 宏观周报

# 可选：webhook 代号——以 HOOK_ 开头命名定义一次，之后所有 WECOM_WEBHOOK*
# 变量里直接写这个变量名（可逗号分隔、可与完整地址混用、代号可套代号 ≤3 层）。
# HOOK_A=https://qyapi.weixin.qq.com/cgi-bin/webhook/send?key=xxxxxxxx
# HOOK_MOYU_XIEHUI=https://qyapi.weixin.qq.com/cgi-bin/webhook/send?key=yyyyyyyy
# WECOM_WEBHOOK_DAILY=HOOK_A
# WECOM_WEBHOOK_GAMES=HOOK_MOYU_XIEHUI   # 游戏资讯推摸鱼协会群

# 可选：每日仓库摘要（daily_update）只统计部分仓库——origin remote URL 含此
# 子串才纳入（如公司内网 git 域名），其余仓库整仓跳过。留空 = 统计全部仓库。
# DAILY_REPO_FILTER=github.corp.example.com

# 可选：覆盖 agent 工作目录（默认 /home/<user>）
# WECHAT_AGENT_CWD=/home/youruser

# 可选：agent 单轮最大步数 / 超时秒数
# WECHAT_AGENT_MAX_TURNS=15
# WECHAT_AGENT_TIMEOUT=120
